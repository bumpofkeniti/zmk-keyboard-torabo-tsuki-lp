#include "torabo_tsuki_lp.dtsi"
#include <input/processors.dtsi>
#include <dt-bindings/zmk/input_transform.h>

/* すべての設定を、以前完璧に動いていた元の正しいルートに100%戻します */
&pointing_listener {
    input-processors = 
        <&zip_xy_transform (INPUT_TRANSFORM_X_INVERT | INPUT_TRANSFORM_Y_INVERT)>,
        <&zip_xy_scaler 1 2>, 
        <&zip_temp_layer 5 1000>; /* 👈 エラーが出ない元の安全な書き方に戻しました */

    /* レイヤー6のときだけスクロールにする設定（左右のみ反転・4分の1速度） */
    scroller {
        layers = <6>;
        input-processors = 
            <&zip_xy_transform (INPUT_TRANSFORM_X_INVERT)>, 
            <&zip_xy_to_scroll_mapper>, 
            <&zip_scroll_scaler 1 4>;
    };
};

/* 💡【これで解決！】エラーの原因になっている古い物理レイアウトの設定を、
   ビルドする直前にこのファイルから上書きしてエラーを強制クリアします。 */
&physical_layout_s { #input-processor-cells = <1>; };
&physical_layout_m { #input-processor-cells = <1>; };
&physical_layout_l { #input-processor-cells = <1>; };

/* エラーの原因になっている古い命令を、空っぽにして無効化（上書き）する */
/ {
    /delete-property/ automouse-layer;
};

&size_s_transform {
    col-offset = <7>;
};

&size_m_transform {
    col-offset = <7>;
};

&size_l_transform {
    col-offset = <7>;
};

