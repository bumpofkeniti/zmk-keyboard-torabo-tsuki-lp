#include "torabo_tsuki_lp.dtsi"
#include <input/processors.dtsi>
#include <dt-bindings/zmk/input_transform.h>

/* トラックボールの入力処理設定 */
&pointing_listener {
    input-processors = 
        <&zip_xy_transform (INPUT_TRANSFORM_X_INVERT | INPUT_TRANSFORM_Y_INVERT)>,
        <&zip_temp_layer 5 1500>,
        <&zip_xy_scaler 3 4>; /* 👈 リストの最後なので、末尾のカンマを外してきれいに閉じました */

    /* レイヤー3のときだけスクロールにする設定 */
    scroller {
        layers = <3>;
        input-processors = <&zip_xy_to_scroll_mapper>, <&zip_scroll_scaler 1 4>; /* 👈 スクロール速度を4分の1に落とす設定 */
    };
};

/* 古い命令を無効化（上書き） */
/ {
    /delete-property/ automouse-layer;
};

&size_s_transform {
    col-offset = <7>;
};

&size_m_transform {
    col-offset = <7>;
};

&size_l_transform {
    col-offset = <7>;
};
