#include "torabo_tsuki_lp.dtsi"
#include <input/processors.dtsi>
#include <dt-bindings/zmk/input_transform.h>

/* 大元の pointing_listener の不足している定義をここで補い、完全に上書きします */
&pointing_listener {
    compatible = "zmk,input-listener";
    #input-processor-cells = <1>; /* 👈 エラー（lacks #input-processor-cells）を解決するために追加 */
    
    input-processors = 
        <&zip_xy_transform (INPUT_TRANSFORM_X_INVERT | INPUT_TRANSFORM_Y_INVERT)>,
        <&zip_xy_scaler 1 2>, 
        /* 1秒維持、15ピクセル以上の移動のみでオートマウス（レイヤー5）を発動 */
        <&zip_temp_layer 5 1000 15>; 

    /* レイヤー6のときだけスクロールにする設定（左右のみ反転・4分の1速度） */
    scroller {
        layers = <6>;
        input-processors = 
            <&zip_xy_transform (INPUT_TRANSFORM_X_INVERT)>, 
            <&zip_xy_to_scroll_mapper>, 
            <&zip_scroll_scaler 1 4>;
    };
};

/* エラーの原因になっている古い命令を、空っぽにして無効化（上書き）する */
/ {
    /delete-property/ automouse-layer;
};

&size_s_transform {
    col-offset = <7>;
};

&size_m_transform {
    col-offset = <7>;
};

&size_l_transform {
    col-offset = <7>;
};
